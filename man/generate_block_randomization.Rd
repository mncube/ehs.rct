% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block_rand.R
\name{generate_block_randomization}
\alias{generate_block_randomization}
\title{Generate Block Randomization for Clinical Trial}
\usage{
generate_block_randomization(
  n_participants,
  n_arms = 2,
  arm_labels = c("A", "B"),
  block_sizes = c(2, 4, 6, 8),
  allocation_ratio = c(1, 1),
  seed = NULL
)
}
\arguments{
\item{n_participants}{Number of participants to randomize}

\item{n_arms}{Number of treatment arms (default: 2)}

\item{arm_labels}{Character vector of arm labels (default: c("A", "B"))}

\item{block_sizes}{Vector of possible block sizes (default: c(2, 4, 6, 8) for 2 arms)}

\item{allocation_ratio}{Vector of allocation ratios (default: c(1, 1) for 1:1)}

\item{seed}{Random seed for reproducibility}
}
\value{
A data frame with participant assignments
}
\description{
This function generates a block randomization sequence for clinical trials
using variable block sizes to prevent prediction while maintaining balance.
}
\examples{
# Simple 1:1 randomization for 62 participants
assignments <- generate_block_randomization(62)

# Custom block sizes and seed
assignments <- generate_block_randomization(
  n_participants = 62,
  block_sizes = c(4, 6, 8),
  seed = 12345
)
}
